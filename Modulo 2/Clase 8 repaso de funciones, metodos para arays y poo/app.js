//import { saludar } from './saludar.js';

//--- Declarativas
// const nombre = prompt('Ingresa tu nombre');
// saludar(nombre);
/*
function suma(a, b) {
  let result = a + b;
  return result;
}

function resta(a, b) {
  let result = a - b;
  return result;
}

function obtenerParametrosYOperacion() {
  const numero1 = parseInt(prompt('Ingresa el primer numero'));
  const numero2 = parseInt(prompt('Ingresa el segundo numero'));
  const resultado = operacion(numero1, numero2);
  printResultToDocument(`El resultado es ${resultado}`);
}

function printResultToDocument(resultado) {
  document.getElementById('result').textContent = resultado;
}

// Anonimas

const dividir = function (a, b) {
  return a / b;
};

// const multiplicar = (a, b) => {
//   return a * b;
// };

// const multiplicar = (a, b) => a * b;

const multiplicar = (a) => a * 5;

//------metodos para arrays
const personas = ['Ale', 'Ale', 'Dani', 'Nico', 'Mauri'];
//filter
// const resultado1 = personas.filter((persona) => persona === 'Ale');
// console.log(resultado1);
//forEach

// personas.forEach((element, i) => {
//   console.log(element, i);
// });

//   find

// const resultado2 = personas.find((element) => element.length === 3);

// console.log(resultado2);

// map
const resultado3 = personas.map((persona, i) => `${persona} ${i}`);
console.log(resultado3);
//some
console.log(personas.some((persona) => persona.length > 5));

const numbers = [44, 2, 5, 1, 3, 55];
numbers.sort((a, b) => {
  return a - b;
});
console.log(numbers);
*/
